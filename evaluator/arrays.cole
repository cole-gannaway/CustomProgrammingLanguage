class Main {
	function main(){
		var size = 10;
		var arr = newArray(size);
		var i = 0;
		while (i < size){
			setArray(arr,i,i);
			i = i + 1;
		}
		i = 0;
		while (i < size){
			print(getArray(arr,i),endl);
			i = i + 1;
		}
	}
}
