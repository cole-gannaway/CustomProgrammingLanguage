class Main {
    function fib(n){
        if (n < 2) { return n; }
        return fib(n-1) + fib (n-2);
    }
	function main(){
        var i = 0;
        var end = 10;
        while (i < end){
            print(fib(i),endl);
            i = i + 1;
        }
	}
}
