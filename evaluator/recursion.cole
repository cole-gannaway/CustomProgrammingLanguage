class Main {
	function fib (n){
        if (n < 2) {
            return n;
        }
        return fib(n-1) + fib(n-2);
	}
	function main(){
		var zero = fib(2);
        print("zero is ", zero, endl);
	}
}
